import { Button } from "../ui/button"
import { Search } from "lucide-react"

const SearchButton = () => {
  return (
    <Button variant={"red_default"} className="h-full">
        <Search/>
    </Button>
  )
}

export default SearchButton
